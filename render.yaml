services:
  - type: web
    name: portfolio-app
    env: docker
    plan: free
    autoDeploy: true
    dockerfilePath: Dockerfile
    healthCheckPath: /health
    # Render will set PORT env; container uses it for uvicorn

